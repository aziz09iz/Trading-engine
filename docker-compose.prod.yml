services:
  api:
    ports: []
    restart: always

  dashboard:
    ports:
      - "${APP_PORT:-80}:80"
    restart: always

  redis:
    ports: []
    restart: always

  postgres:
    ports: []
    restart: always
